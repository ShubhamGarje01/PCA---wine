Performing a Principal component analysis and clustering using first 
3 principal component scores (both heirarchial and k mean clustering(scree plot or elbow curve))
and try ing to obtain  the optimum number of clusters and checking whether we have obtained same number of clusters with the original data.

(class column we have ignored at the begining who shows it has 3 clusters)
